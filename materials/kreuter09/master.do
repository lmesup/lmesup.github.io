// Analysis of contact sequences of ESS
// ------------------------------------
// kohler@wzb.eu, fkreuter@survey.umd.edu

do cr_ESSseqa_result // first sketch on contact data
do crESScontact      // dataset for Prague conference, ESS 02 only
do andescribtion_by_country // Some descriptions by country
do anseqdes          // Description of sequences
do ancorrelation     // Correlation with fieldwork vars (time of int. etc.)
do ansqom1           // Optimal matching of sequences
do crESScontactbig   // ESS'02 and ESS'04
do crESSdata         // File with substanial vars
do ansq_by_response  // SQ Descript. by resp. status, contactab., and subst.vars
do ansqom_by_response  // OM by resp. status, contactab., and subst.vars

// Rework before 3MC
do anseqdes1        // Description of sequences

// Rework after 3MC
do anseqdes2        // Description of sequences
do ansqom_with_dresult  // Try with detailed results. -> Give up OM
do anintprob        // Correlation with interv. probability

do anseqdes3   // Sequence length of 3 and longer
do anintprob1  // Corr with interv. prob. (sequence length of 3+)
do ankeyvars1   // Correlation with key survey variables
do anweights   // Internal criteria of representativity

do crESScontact1-3 // Add data from 3rd round
do anseqdes4   // Sequence length of 3 and longer, rounds 1-3
do crESSdata1-3 // Add data for 3rd round
do anintprob2  // Corr with interv. prob., rounds 1-3
do ankeyvars2  // Corrlation with key survey vars, rounds 1-3
do anintprob3  // Corr with interv. prob. without emp and tvtot
do ankeyvars3  // Corrlation with key survey vars, (new list of keyvars)

// Rework after JOS review

















